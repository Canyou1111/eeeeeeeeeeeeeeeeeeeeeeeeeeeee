# generate_island.mcfunction
# This function is a helper to place the End main-island structures if run by an operator.
# It is intentionally inert to avoid accidental overwriting of worlds when the datapack is installed.
# To use it, run (as an operator):
# /execute in the_end run function endergizedendisland:generate_island_force

